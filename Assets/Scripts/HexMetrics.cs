using UnityEngine;

public static class HexMetrics 
{
	public const float outerRadius = 0.4f;
    //public const float innerRadius = outerRadius * 0.866025404f;
    public const float innerRadius = outerRadius * 0.7368f;
}